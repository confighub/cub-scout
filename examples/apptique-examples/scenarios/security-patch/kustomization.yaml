apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Security Patch Scenario
# Deployments with known CCVEs for scanning demo
# - no-limits: Missing resource limits
# - latest-tag: Using :latest tag
# - grafana-ccve: BIGBANK Grafana sidecar bug

resources:
  - namespace.yaml
  - deployment-no-limits.yaml
  - deployment-latest-tag.yaml
  - deployment-grafana-ccve.yaml
