# Kustomize base for database (PostgreSQL via Helm)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
commonLabels:
  app.kubernetes.io/name: postgresql
  app.kubernetes.io/part-of: platform
