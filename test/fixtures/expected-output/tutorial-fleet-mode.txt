
   🔥 5 FAILURE(S)   atk

  Deployers  0/3
  Workloads  13/16

  PROBLEMS
  ────────────────────────────────────────────────
✗ HelmRelease/redis-cache  SourceNotReady
✗ Application/frontend-app  null
⏸ Kustomization/monitoring-stack  suspended
✗ demo-orders/order-processor  0/2 pods
✗ demo-payments/frontend  0/2 pods
✗ demo-payments/payment-api  0/3 pods

  PIPELINES
  ────────────────────────────────────────────────
⏸ company/infrastructure@main  →  monitoring-stack  →  0 resources
✗ company/frontend/k8s@HEAD  →  frontend-app  →  demo-payments

  OWNERSHIP
  ────────────────────────────────────────────────
  Argo(1) ConfigHub(2) Helm(1) Native(12)
  ████░░░░░░░░░░░░

  ConfigHub Fleet View
  Hierarchy: Application → Variant → Cluster

  unknown
  ├── variant: default
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev Detached
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? - @ rev Detached
  │   └── ? - @ rev Detached
  │   └── ? - @ rev Detached
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev NotLive
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev NotLive
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sweet-growl-traderx-worker-dev @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-tck-worker @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-tck-worker @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? cluster-target @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? cluster-target @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? worker @ rev NotLive
  │   └── ? k8s-dev @ rev Ready
  │   └── ? k8s-dev @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? k8s-salmon-cub-traderx-worker-dev @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? k8s-sleepy-snout-traderx-worker-dev @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-devops-test-worker @ rev Progressing
  │   └── ? k8s-devops-test-worker @ rev Ready
  │   └── ? k8s-devops-test-worker @ rev Ready
  │   └── ? k8s-devops-test-worker @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-mellow-muzzle-traderx-worker @ rev Ready
  │   └── ? k8s-mellow-muzzle-traderx-worker @ rev NotLive
  │   └── ? k8s-mellow-muzzle-traderx-worker @ rev NotLive
  │   └── ? k8s-mellow-muzzle-traderx-worker @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? k8s-worker-us @ rev Ready
  │   └── ? k8s-worker-us @ rev Progressing
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-forest-cub-traderx-worker @ rev Ready
  │   └── ? k8s-forest-cub-traderx-worker @ rev Ready
  │   └── ? k8s-forest-cub-traderx-worker @ rev Ready
  │   └── ? k8s-forest-cub-traderx-worker @ rev Degraded
  │   └── ? - @ rev NotLive
  │   └── ? - @ rev NotLive
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? - @ rev NotLive
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? k8s-worker-infra @ rev Ready
  │   └── ? k8s-worker-us @ rev Ready
  │   └── ? k8s-worker-us @ rev Degraded
  │   └── ? k8s-worker-eu @ rev Ready
  │   └── ? k8s-worker-eu @ rev Degraded
  │   └── ? k8s-worker-asia @ rev Ready
  │   └── ? k8s-worker-asia @ rev Degraded
  │   └── ? qa @ rev NotLive
  │   └── ? qa @ rev NotLive
  │   └── ? qa @ rev NotLive
  │   └── ? qa @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? us @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? eu @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive
  │   └── ? asia @ rev NotLive


